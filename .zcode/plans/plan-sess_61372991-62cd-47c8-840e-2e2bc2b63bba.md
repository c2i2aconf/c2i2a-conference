Add the HEEC Marrakech Google Maps map to the site, driven by CMS data.

**Place**: HEEC Marrakech — Avenue Allal El Fassi, Rue Abou Oubaida Al Jarah, Daoudiate, Marrakech (31.6465301, -7.9946813 from the shared link). Embed URL (keyless): `https://maps.google.com/maps?q=HEEC+Marrakech&z=15&output=embed` (verify visually; fall back to `?q=31.6465301,-7.9946813` if the place-name query resolves wrong).

## Changes

1. **New `src/components/sections/MapEmbed.tsx`** — server component (`url?: string | null`, `title: string`, optional `className`). Encapsulates the embed detection currently inlined in the access page (`url.includes('google') && url.includes('embed')`), the rounded bordered frame, lazy-loading iframe (`referrerPolicy="no-referrer-when-downgrade"`, `allowFullScreen`), and the `MapPin` placeholder fallback.

2. **`src/app/(frontend)/[locale]/access/page.tsx`** — replace the inline embed-detection + iframe block with `<MapEmbed url={edition?.venueMapUrl} title={t('map')} />`. Venue card and directions button unchanged.

3. **`src/app/(frontend)/[locale]/page.tsx` (venue teaser, lines 407-432)** — keep the centered blue-band layout; add `<MapEmbed>` below the CTA inside `mx-auto mt-10 max-w-3xl`, ~360px tall, so the homepage shows the live map.

4. **`src/seed/index.ts`** — in the live-edition create: add `venueMapUrl` (embed URL, non-localized) and `venueAddress` fr/en ("Avenue Allal El Fassi, Rue Abou Oubaida Al Jarah, Daoudiate\nMarrakech, Maroc" / "...Marrakech, Morocco"). In the "edition already exists" branch, backfill `venueMapUrl`/`venueAddress` only when empty (idempotent).

No collection/schema changes → no `generate:types`, no migration needed.

## Verification
- `npx tsc --noEmit` and `npm run lint`
- `npm run seed`, then dev server + browser screenshots of `/fr/access` and the homepage venue section to confirm the iframe loads the correct HEEC pin

## Production note
Vercel never runs the seed: on the deployed site the embed URL must be set once in `/admin` (Editions → live edition → Venue map URL). Final summary will include the exact URL to paste.